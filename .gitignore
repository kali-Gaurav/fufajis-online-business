# ================================================================
# FUFAJI ONLINE BUSINESS - COMPREHENSIVE .GITIGNORE
# This file prevents sensitive data from being committed to git
# ================================================================

# ── Flutter & Dart ─────────────────────────────────────────────
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
pubspec.lock
*.iml

# ── Build Artifacts ───────────────────────────────────────────
build/
dist/
.gradle/
*.apk
*.aab
*.ipa
.build/
coverage/

# ── Environment Variables - NEVER commit these ─────────────────
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local
.env.staging
.env.production
.env.development
*.env.js
env.js

# ── Secrets and Credentials ────────────────────────────────────
.secrets
secrets/
private/
keys/
.credentials
credentials.json
config/secrets.js
config/secrets.json
config/production.js
config/staging.js

# ── Keystore and Signing Keys - CRITICAL: NEVER COMMIT ─────────
*.jks
*.keystore
*.p12
*.pfx
*.pem
*.key
signing-key.jks
upload-keystore.jks
fufaji-upload-key.jks
fufaji-native.jks
android/upload-keystore.jks
android/key.properties
android/app/fufaji-upload-key.jks
android/fufaji-upload-key.jks
app/fufaji-app/fufaji-upload-key.jks
app/fufaji-app/fufaji-native.jks

# ── Firebase Credentials - NEVER COMMIT ────────────────────────
google-services.json
google-services-*.json
firebase-adminsdk-*.json
firebase-config.json
firebase-key.json
serviceAccountKey.json
GoogleService-Info.plist

# ── AWS / Cloud Credentials ────────────────────────────────────
.aws/
aws-credentials.json
aws-secret-key.*
aws-config.json

# ── Supabase Credentials ───────────────────────────────────────
.supabase-credentials
supabase-key.json

# ── Private Keys ───────────────────────────────────────────────
private_key*
id_rsa*
id_dsa*
*.ppk
ssh-keys/
rsa_keys/

# ── API Keys and Tokens ────────────────────────────────────────
*api_key*
*api_secret*
*access_token*
*refresh_token*
*webhook_secret*
*webhook_url*
razorpay-secrets*
stripe-keys*
twilio-credentials*

# ── Android Build Files ───────────────────────────────────────
android/.gradle/
android/local.properties
android/key.properties
android/gradlew.bat
android/gradlew

# ── Node Dependencies ─────────────────────────────────────────
node_modules/
functions/node_modules/
backend/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── IDE & Editor ──────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
*.sublime-workspace
*.sublime-project
.vs/
.vscode/settings.json

# ── Logs ──────────────────────────────────────────────────────
logs/
*.log
npm-debug.log*
yarn-debug.log*
lerna-debug.log*

# ── Dependencies & Packages ──────────────────────────────────
/flutter/.pub-cache/
/flutter/pubspec.lock
node_modules/
.package-lock.json
Pods/
.cocoapods/

# ── macOS ─────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── Windows ───────────────────────────────────────────────────
Thumbs.db
ehthumbs.db
*.lnk

# ── Linux ─────────────────────────────────────────────────────
.directory

# ── Temporary Files ───────────────────────────────────────────
*.tmp
*.bak
*.temp
*.orig
*.patch~

# ── Firebase Emulator ─────────────────────────────────────────
ui-debug.log
**/firebase-debug.log

# ── Test Coverage ─────────────────────────────────────────────
coverage/
.nyc_output/
lcov.info

# ── System Files ──────────────────────────────────────────────
.cache/
.run/
.venv/
venv/
env/
ENV/

